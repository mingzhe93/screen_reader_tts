{
  "name": "voicereader-desktop",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "sidecar:build": "python ./tts-engine/scripts/build_sidecar.py",
    "desktop:dev": "tauri dev",
    "desktop:build": "tauri build",
    "desktop:build:standalone": "tauri build",
    "desktop:build:portable": "npm run sidecar:build && npm run build && tauri build --bundles none && python ./scripts/package_portable.py"
  },
  "dependencies": {
    "@tauri-apps/api": "^1.6.0"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^1.6.0",
    "typescript": "^5.9.2",
    "vite": "^5.4.11"
  }
}
